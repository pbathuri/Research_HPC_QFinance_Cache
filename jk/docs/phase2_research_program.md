# Phase 2 Research Program

This document defines a disciplined continuation after paper packaging.

## Tracks

1. x86 PMU validation track (`ready for x86/HPC validation`)
2. BigRed200 scaling track (`ready for BigRed200 execution planning`)
3. similarity-caching validation track (`ready for x86/HPC validation`)
4. guided-cache systems track (`deferred / speculative` until stronger PMU evidence)
5. QHPC mapping track (`conceptually mappable to QHPC later`)

## Time horizons

- Near-term: PMU and similarity replay validation
- Medium-term: BigRed200 scaling and guided-cache systems experiments
- Long-term: hybrid QHPC mapping experiments

## Output anchor

- `phase2_research_program_summary.csv`

