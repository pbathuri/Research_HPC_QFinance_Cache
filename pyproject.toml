[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "qhpc-cache"
version = "0.1.0"
description = "Monte Carlo pricing and cache prototype"

[tool.setuptools.packages.find]
where = ["src"]
